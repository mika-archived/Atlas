{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "predeploy": "yarn run build",
    "source": "functions"
  },
  "storage": {
    "rules": "storage.rules"
  }
}